import React from 'react';

interface FoodImageGeneratorProps {
  onImageGenerated?: (url: string) => void;
}

export const FoodImageGenerator: React.FC<FoodImageGeneratorProps> = () => {
  return null;
}